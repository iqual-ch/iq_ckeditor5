# Iqual Ckeditor5 Configuration utility

This module is an utility to facilitate ckeditor5 extensions and configuration to all projects.

It should not be part of any project requirements but rather used in payload during update or upgrade workflow.
